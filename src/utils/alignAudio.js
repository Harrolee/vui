const alignAudio = (track1, track2) => {
  // find the signature

  // trim both tracks to the earliest signature

  // return trimmed tracks

  return {
    one: track1,
    two: track2,
  };
};

export default alignAudio;
